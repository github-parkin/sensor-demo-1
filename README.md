# sensor-demo-1
Small Python script that generates simulated temperature and humidity data using NumPy and visualises it with pandas and matplotlib

## Files

- `sensor_plot.py` – script that generates data and saves a plot (`sensor_plot.png`).

## How to run

Install the required packages:

```bash
pip install numpy pandas matplotlib

## XYW
``` xyz
